is_cycle_dimming:
  fields:
    lights:
      required: true
      selector:
        entity:
          multiple: true
          filter:
            domain: light

is_ccw_cycling:
  fields:
    lights:
      required: true
      selector:
        entity:
          multiple: true
          filter:
            domain: light
